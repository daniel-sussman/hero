<div class="container sign-page">
  <div class="row">
    <div class="col-12">
        <h2>Great, thanks for signing up!</h2>
        <p>Just a couple more questions to help us show you the most relevant results. First off, how many children do you have?</p>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>

          <div class="form-inputs">
            <%= f.input :children,
                        collection: 0..8, prompt: "Select a number..." %>
            <div id="birthday_fields"></div>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Submit", :class=> 'btn-info mb-3' %>
          </div>
        <% end %>
    </div>
    <div class="col-12">
        <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
